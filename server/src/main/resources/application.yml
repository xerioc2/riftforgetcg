server:
  port: ${PORT:8080}

riftforge:
  riftcodex-api: https://api.riftcodex.com/cards
  target-score: 8
  cors-origins: ${CORS_ORIGINS:http://localhost:5173,http://tauri.localhost,tauri://localhost,https://tauri.localhost}
