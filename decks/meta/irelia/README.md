# Irelia Meta Deck Notes

The supplied Irelia guide lists are now extracted as JSON in this directory.

The repo still has `Irelia Tempo` in `src/lib/starterDecks.ts`; keep treating
that as a playtest/dev list. Use `docs/meta/irelia.md` for guide-list blockers
when choosing the next Irelia implementation slice.

Do not claim competitive-meta support until the guide-list blockers are fixed
and directly tested.
