# Master Yi Meta Deck Notes

The supplied Master Yi guide lists are now extracted as JSON in this directory.

Master Yi is the raw meta leader in the supplied numbers, but reviewer signal
says implementation should wait until someone can describe the deck's actual
gameplay and must-work cards.

Use `docs/meta/master-yi.md` for the extracted support audit, but still collect
gameplay notes before choosing implementation work. Do not implement from the
archetype name alone.
